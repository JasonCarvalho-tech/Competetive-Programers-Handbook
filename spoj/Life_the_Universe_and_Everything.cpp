// URL: https://www.spoj.com/problems/TEST/
#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(NULL);

	string x;
	while (cin >> x) {
        if (x == "42") {
            break;
        }
		cout << x << endl;
	}
}
/**
 * very basic problem a intro on how to take input and output
*/
